# 로컬 실행 의존성입니다. uv를 사용하면 pyproject.toml을 설치하세요.
# SageMaker 학습 컨테이너 의존성은 tracks/*/scripts/requirements.txt에 있습니다.
#
# LiteLLM은 sagemaker와 의존성 충돌이 있어 별도 환경에 설치합니다.

# AWS와 SageMaker
sagemaker>=3.16.0
boto3>=1.43.51

# 로컬 데이터 준비와 dry-run 학습
transformers==5.14.1
datasets>=5.0.0
huggingface_hub>=1.24.0

# 로컬 GPU 학습
trl>=1.8.0
peft>=0.19.1
accelerate>=1.14.0
bitsandbytes>=0.49.2        # QLoRA(4bit)용

# Strands
strands-agents>=1.48.0
strands-agents-tools>=0.8.4
# bedrock-agentcore>=1.18.1        # AgentCore Runtime 배포 시 (05 노트북)
# langchain-aws>=1.6.2             # LangGraph 옵션 사용 시

# 노트북
jupyterlab
ipywidgets
