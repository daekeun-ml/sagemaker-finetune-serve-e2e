# SageMaker HF DLC의 학습 컨테이너에서 설치하는 추가 의존성입니다.
# 로컬 환경과 별개이며 DLC의 torch, CUDA와 호환되는 버전을 사용해야 합니다.
# transformers 5.15.0은 생성한 config를 현재 서빙 이미지가 읽지 못해 5.14.1로 고정합니다.
# 버전을 바꿀 때는 학습과 배포를 함께 검증하세요.
transformers==5.14.1
# TRL 버전별 인자 차이는 train_grpo.py에서 지원 여부를 확인해 처리합니다.
trl>=1.8.0
peft>=0.19.1
datasets>=5.0.0
accelerate>=1.0.0
bitsandbytes>=0.44.0     # QLoRA 4비트 학습에만 필요합니다.

# 실험 추적은 MLFLOW_TRACKING_URI가 전달된 학습 작업에서만 사용합니다.
# 모델 파일을 기록하지 않으므로 mlflow-skinny만 설치합니다.
# sagemaker-mlflow는 SageMaker ARN과 SigV4 인증을 지원합니다.
mlflow-skinny>=3.10
sagemaker-mlflow>=0.5.0
# 시스템 지표 수집에 필요한 의존성입니다.
psutil>=5.9.0
nvidia-ml-py>=12.535.77
